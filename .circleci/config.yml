Skip to content
Why GitHub? 
Team
Enterprise
Explore 
Marketplace
Pricing 
Search

Sign in
Sign up
Learn Git and GitHub without any code!
Using the Hello World guide, you’ll start a branch, write comments, and open a pull request.


cpsingh111
/
cleancode_CNCMonitoring
1
00
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
cleancode_CNCMonitoring/.circleci/config.yml
@cpsingh111
cpsingh111 CCN increased to 5
Latest commit 32035e2 on Jul 23
 History
 1 contributor
44 lines (44 sloc)  1.29 KB
  
version: 2.1
jobs:
  build:
    docker:
      - image: "debian:stretch"
    steps:
      - checkout
      - run:
          name: Installing SUDO
          command: 'apt-get update && apt-get install -y sudo && rm -rf /var/lib/apt/lists/*'
      - run:
          name: Installing GCC
          command: 'apt-get update && apt-get install -y gcc g++'
      - run:
          name: Install GTest
          command: |
            apt-get update && apt-get install -y libgtest-dev
            apt-get install -y cmake
            cd /usr/src/gtest
            cmake CMakeLists.txt
            make
            cp -a include/gtest /usr/include  
            cp *.a /usr/lib
      - run:
          name: Install cppcheck
          command: 'apt-get update && apt-get install -y cppcheck'
      - run:
          name: Install pip
          command: 'apt-get update && apt-get install -y python-pip'
      - run:
          name: Install lizard
          command: 'pip install lizard'
      - run:
          name: Create Build Files
          command: 'make'
      - run:
          name: Static Analysis
          command: 'cppcheck --enable=all --error-exitcode=1 .'
      - run:
          name: Cyclomatic Complexity Check
          command: 'lizard --CCN 5'
      - run:
          name: Unit test
          command: './testcheck'
© 2020 GitHub, Inc.
Terms
Privacy
Security
Status
Help
Contact GitHub
Pricing
API
Training
Blog
About
